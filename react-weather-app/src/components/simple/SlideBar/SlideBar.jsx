import BarContainer from "./BarContainer";
import ExitButton from "../../base/ExitButton";
import Heading from "./Heading";

export default function SlideBar({ children }) {
  return <>{children}</>;
}
